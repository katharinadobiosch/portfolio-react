import styles from "../styles/_loadingBar.module.scss";

const LoadingBar = () => {
    return (
        <>
        
            <div className={styles.slide}></div>
        </>
    );
};

export default LoadingBar;
